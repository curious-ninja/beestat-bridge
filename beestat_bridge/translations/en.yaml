configuration:
  mode:
    name: Default data source
    description: >-
      cloud = pass through to the real ecobee API (and archive everything
      locally). local = serve beestat from Home Assistant data only. This is
      only the default — switch live, and configure everything else, from
      the Beestat Bridge page in the sidebar.
