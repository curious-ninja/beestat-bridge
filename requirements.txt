fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
PyYAML>=6.0
python-multipart>=0.0.9
# TODO(bridge): interactive consumer login. Same library HA core's ecobee
# integration uses for keyless auth; pinned once the exact auth surface is
# wired in ecobee_auth.py.
# python-ecobee-api
